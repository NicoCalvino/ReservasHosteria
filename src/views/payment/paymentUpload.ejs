<%- include('../partials/header',{hojaDeEstilo:"/css/stylesBookings.css"}) %>
<div class="hero nc-timbre-tris" id="reserva-timbre">
    <div class="container">
        <div class="main-heading">
            <h1 class="title-res nc-titulo-pagina"><%= __('payment_upload.titulo') %></h1>
            <% if(locals.mensajePagina ){ %>
                <div class="nc-error-block">
                    <label class="nc-error-message"><%= mensajePagina %></label>   
                </div>
            <% } %>
            <div class="nc-block">
                <form action="/payment/upload" class="form nc-payment-form" method="POST" enctype="multipart/form-data">
                    <div class="nc-file-info-div">
                        <label class="nc-file-info-label" for="booking_code"><%= __('payment_upload.codigo') %></label>
                        <input class="nc-file-input" type="text" id="booking_code" name="booking_code" <%=locals.oldInfo && oldInfo.booking_code ? "value=" + oldInfo.booking_code : '' %>>
                        <% if(locals.errors && errors.booking_code){ %>
                            <div class="nc-error-block">
                                <label class="nc-error-message"><%= errors.booking_code.msg %></label>   
                            </div>
                        <% } %>
                    </div>
                    <div class="nc-file-info-div">
                        <label class="nc-file-info-label" for="email"><%= __('payment_upload.email') %></label>
                        <input class="nc-file-input" type="email" id="email" name="email" <%=locals.oldInfo && oldInfo.email ? "value=" + oldInfo.email : '' %>>
                        <% if(locals.errors && errors.email){ %>
                            <div class="nc-error-block">
                                <label class="nc-error-message"><%= errors.email.msg %></label>   
                            </div>
                        <% } %>
                    </div>
                    <div class="nc-file-info-div">
                        <label class="nc-file-info-label" for="receipt"><%= __('payment_upload.comprobante') %></label>
                        <input class="nc-file-input" type="file" id="receipt" name="receipt">
                        <% if(locals.errors && errors.receipt){ %>
                            <div class="nc-error-block">
                                <label class="nc-error-message"><%= errors.receipt.msg %></label>   
                            </div>
                        <% } %>
                    </div>
                    <div class="nc-button-block nc-payment-button">
                        <input class="btn btn-gradient nc-btn-extra" type="submit" value="<%= __('payment_upload.boton') %>">
                    </div>
                </form>
                <div class="nc-comunication-block">
                    <span><%= __('payment_upload.msg_olvido') %></span>
                </div>
            </div>
        </div>
    </div>
</div>
  <section class="booking">
  </section>
</main>
<%- include('../partials/footer') %>